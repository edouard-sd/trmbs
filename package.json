{
  "name": "trombinoscope-app",
  "private": true,
  "version": "1.0.0",
  "description": "App d'apprentissage de trombinoscopes associatifs",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "firebase": "^11.4.0"
  }
}
